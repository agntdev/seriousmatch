[
  {
    "name": "⚙️ Настройки",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Open settings (notification prefs, basic search prefs)."
            }
          }
        ],
        "send": {
          "callback": "settings:open"
        }
      }
    ]
  }
]
